<div id="home">
  <div class="container">
    <div class="row">
      <div class="col-8 offset-2">
        <h1>{{msg}}</h1>
        <h2>This is your Industry 4.0 workbench!</h2>

        </form>-->
       <form class="clearfix mb-4" action="/addprocess">
            <button id="add-new-process" class="btn btn-primary float-right">Add New Process</button>
        </form>
        <br>
        <h3>Choose a process to monitor:</h3>
        <ul id="process-list" class="list-group">
          {{#each process}}
            <li data-id="{{this.id}}" class="list-group-item">
            <a href="process/{{this.id}}">{{this.Process_name}}</a>
            <button class="btn btn-danger float-right delete">delete</button>
          </li>
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="/js/index.js"></script>
